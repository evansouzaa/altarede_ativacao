import styled from "styled-components";

export const FormStyled = styled.form`
    
    input, select {
        width: 100%;
    }

    .button-box {
        display: flex;
        justify-content: space-evenly;
        margin: 15px;
    }

    Button {
        width: 85px;
    }

    .mb-1 > label {
        font-size: 0.7rem;
    }
`